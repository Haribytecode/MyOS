.code32

.global switch_context

/*
 * void switch_context(context_t **old, context_t *new);
 *
 * NOT IMPLEMENTED YET.
 * This is only a placeholder so the ABI is frozen.
 */

switch_context:
    ret

.section .note.GNU-stack,"",@progbits